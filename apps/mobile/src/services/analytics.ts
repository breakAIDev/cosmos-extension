// Simple placeholder for analytics
export const logEvent = (event: string, params?: Record<string, any>) => {
  // Integrate with analytics SDK (Firebase, Mixpanel, etc)
  console.log("Analytics event:", event, params);
};
